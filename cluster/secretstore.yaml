apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: fake
  annotations:
    argocd.argoproj.io/sync-wave: "-80"
spec:
  provider:
    fake:
      data:
        - key: test
          value: hi
